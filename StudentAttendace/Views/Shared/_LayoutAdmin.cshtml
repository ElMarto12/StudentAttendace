<!DOCTYPE html>
<html lang="lt">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>@ViewData["Title"]Administratorius</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true"/>
</head>
<body>
<main class="d-flex flex-nowrap custom-bg-lightblue">
    <div class="d-flex flex-column flex-shrink-0 p-3 custom-bg-blue sidebar">
        <a class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
            <span class="fs-4">Administratorius</span>
        </a>
        <hr>
        <ul class="nav nav-pills flex-column mb-auto">
            <li class="nav-item">
                <a class="nav-link link-body-emphasis @(ViewContext.RouteData.Values["controller"]?.ToString() == "Admin" && ViewContext.RouteData.Values["action"]?.ToString() == "AdminLecture" ? "active-button-blue text-light" : "")"
                   asp-controller="Admin" asp-action="AdminLecture">
                    <i class="bi bi-book me-2"></i>
                    Paskaitos
                </a>
            </li>
            <li>
                <a class="nav-link link-body-emphasis @(ViewContext.RouteData.Values["controller"]?.ToString() == "Admin" && ViewContext.RouteData.Values["action"]?.ToString() == "AdminGroup" ? "active-button-blue text-light" : "")"
                   asp-controller="Admin" asp-action="AdminGroup">
                    <i class="bi bi-person me-2"></i>
                    Grupės
                </a>
            </li>
            <li>
                <a class="nav-link link-body-emphasis  @(ViewContext.RouteData.Values["controller"]?.ToString() == "Admin" && ViewContext.RouteData.Values["action"]?.ToString() == "AdminStudent" ? "active-button-blue text-light" : "")"
                   asp-controller="Admin" asp-action="AdminStudent">
                    <i class="bi bi-people me-2"></i>
                    Studentai
                </a>
            </li>
            <li>
                <a class="nav-link link-body-emphasis  @(ViewContext.RouteData.Values["controller"]?.ToString() == "Admin" && ViewContext.RouteData.Values["action"]?.ToString() == "AdminTeacher" ? "active-button-blue text-light" : "")"
                   asp-controller="Admin" asp-action="AdminTeacher">
                    <i class="bi bi-person-vcard me-2"></i>
                    Dėstytojai
                </a>
            </li>
            <li>
                <a class="nav-link link-body-emphasis  @(ViewContext.RouteData.Values["controller"]?.ToString() == "Admin" && ViewContext.RouteData.Values["action"]?.ToString() == "AdminSubject" ? "active-button-blue text-light" : "")"
                   asp-controller="Admin" asp-action="AdminSubject">
                    <i class="bi bi-file-text me-2"></i>
                    Studijų Dalykai
                </a>
            </li>
        </ul>
        <hr>
        <form method="post" class="d-inline-flex" asp-controller="Login" asp-action="LogOut">
            <button type="submit" class="btn">
                <i class="bi bi-box-arrow-left"></i>
            </button>
            <p class="my-2 mx-1">Atsijungti</p>
        </form>
    </div>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>
</main>
<script crossorigin="anonymous" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/js/site.js" asp-append-version="true"></script>
@await RenderSectionAsync("Scripts", required: false)
</body>
</html>